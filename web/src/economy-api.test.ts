import { expect,it } from 'vitest';
import { amountUnits,parseDaily,parsePending,parseTransaction,parseTransactions } from './economy-api';
export const dailyFixture={user_id:'1',business_date:'2026-09-05',timezone:'Asia/Shanghai',next_reset_at:'2026-09-05T16:00:00Z',amount:'500',amount_units:'250000000',asset:'RESERVE_API_CREDIT',policy_version:'1',claimed:false,transaction_id:null};
export const receiptFixture={id:'01990000-1111-7777-aaaa-000000000001',user_id:'1',biz_id:'daily:1:2026-09-05',kind:'DAILY_REWARD',status:'CONFIRMED',from_asset:'',to_asset:'RESERVE_API_CREDIT',amount_units:'250000000',amount:'500',created_at:'2026-09-05T12:00:00Z',confirmed_at:'2026-09-05T12:00:00Z'};
it('keeps exact atomic arithmetic and rejects lossy or overflowing quantities',()=>{expect(amountUnits('0.000002')).toBe(1n);expect(amountUnits('18446744073709.551614')).toBe(9223372036854775807n);for(const v of ['0','0.000001','1e3','-1','NaN','18446744073709.551616'])expect(amountUnits(v)).toBeNull()});
it('accepts only owned confirmed receipts and fixed daily policy',()=>{expect(parseDaily(dailyFixture,'1').amount).toBe('500');expect(parseTransaction(receiptFixture,'1').amount).toBe('500');for(const bad of [{...receiptFixture,user_id:'2'},{...receiptFixture,amount:'501'},{...receiptFixture,kind:'LOCAL_EXCHANGE'},{...receiptFixture,status:'PENDING'}])expect(()=>parseTransaction(bad,'1')).toThrow();expect(()=>parseDaily({...dailyFixture,claimed:true},'1')).toThrow();expect(()=>parseDaily({...dailyFixture,amount:'1000'},'1')).toThrow()});
it('validates persisted requests and transaction pagination',()=>{const p={kind:'EXCHANGE',key:'01990000-1111-7777-aaaa-000000000001',from_asset:'AVAILABLE_CHIPS',amount:'0.000002'};expect(parsePending(JSON.stringify(p))).toEqual(p);expect(()=>parsePending(JSON.stringify({...p,amount:'0.000001'}))).toThrow();expect(parseTransactions({items:[receiptFixture],has_more:false,next_after_id:null},'1').items).toHaveLength(1);expect(()=>parseTransactions({items:[receiptFixture,receiptFixture],has_more:false,next_after_id:null},'1')).toThrow()});
